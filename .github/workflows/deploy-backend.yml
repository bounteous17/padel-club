name: Deploy Backend (Lambda)

on:
  push:
    branches: [master]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: eu-central-1

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: prod

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        working-directory: backend
        run: npm ci

      - name: Build TypeScript
        working-directory: backend
        run: npm run build

      - name: Copy package files and Prisma schema to dist
        working-directory: backend
        run: |
          cp package.json package-lock.json dist/
          cp -r prisma dist/

      - name: Install production dependencies in dist
        working-directory: backend/dist
        run: npm ci --omit=dev --ignore-scripts --platform=linux --arch=x64

      - name: Generate Prisma client in dist
        working-directory: backend/dist
        run: npx prisma generate

      - name: Create Lambda deployment package
        working-directory: backend/dist
        run: |
          zip -r ../lambda.zip . -x "*.map" "*.ts" "*.md"
          echo "Lambda package size: $(du -h ../lambda.zip | cut -f1)"

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Upload Lambda package to S3
        working-directory: backend
        run: |
          aws s3 cp lambda.zip s3://${{ vars.S3_BUCKET_NAME }}/lambda-deployments/lambda.zip
          echo "Lambda package uploaded to S3"

      - name: Deploy to Lambda from S3
        working-directory: backend
        run: |
          aws lambda update-function-code \
            --function-name ${{ vars.LAMBDA_FUNCTION_NAME }} \
            --s3-bucket ${{ vars.S3_BUCKET_NAME }} \
            --s3-key lambda-deployments/lambda.zip
          
          echo "Waiting for Lambda update to complete..."
          aws lambda wait function-updated \
            --function-name ${{ vars.LAMBDA_FUNCTION_NAME }}
          
          echo "Lambda function deployed successfully!"
          echo "Function: ${{ vars.LAMBDA_FUNCTION_NAME }}"
          echo "API URL: https://${{ vars.API_DOMAIN }}"
